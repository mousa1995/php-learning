<?php

$numbers = [10, 20, 30, 40];

foreach ($numbers as $number) {
    echo $number . "\n";
}

$user = [
    "name" => "Mousa",
    "age" => 32,
    "job" => "Developer",
];

foreach ($user as $key => $value) {
    echo "$key: $value\n";
}
